# Risks and Mitigation

Date | Risk | Severity | Mitigation | Owner | Status
---- | ---- | -------- | ---------- | ----- | ------
YYYY-MM-DD | Reproducibility gaps between notebooks and report figures | High | Lock seeds, save configs, export figure scripts | - | Open
2026-04-11 | LinearSVC convergence warnings in Method A may reduce optimization confidence | Medium | Tune solver settings (max_iter=30000, dual=False, tol=1e-4), rerun 3 seeds, publish before/after warning table | Copilot | Accepted for Task1 scope (performance stable; no conclusion change)
2026-04-14 | Fairness challenge if only method-best view is reported | High | Report both method-best and fixed-same-k views in all comparisons | Copilot | Mitigating
2026-04-14 | Incomplete reproducibility metadata may block result audit | High | Enforce six minimum review fields in every run log (preprocess version, search space, seed set, feature list, software versions, selection rule) | Copilot | Mitigating
2026-04-14 | UMAP visual impression may conflict with quantitative metrics | Medium | Treat UMAP as explanatory only; base conclusions on Silhouette/DBI/CHI plus multi-seed mean+-std | Copilot | Mitigating
2026-04-14 | `bribe` feature may create shortcut-like separability and bias clustering conclusions | High | Run separate with-bribe ablation in isolated output root and compare against main no-bribe baseline before final write-up (completed 2026-04-14) | Copilot | Mitigating
2026-04-18 | Fanless MBA thermal throttling may distort long-run RL timing and throughput | High | Use batched queue execution (`max_parallel<=3`, default 2) and fixed 30s cooldown between batches | Copilot | Mitigating
2026-04-18 | Missing per-run model artifacts could silently corrupt best-trajectory evidence | High | Enforce strict export mode in official runner (`--strict true`) and fail pipeline on missing metrics/model files | Copilot | Mitigating
2026-04-18 | 1M-step fixed hyperparameters may reduce late-stage convergence quality | Medium | Enable explicit decay configuration (DQN phased LR+buffer decay, PPO LR decay) and log decay metadata per run | Copilot | Mitigating
2026-04-18 | SB3 phase logs may produce empty CSV slices causing brittle post-processing | Medium | Add defensive CSV reads and always emit fallback loss/progress files | Copilot | Mitigating
2026-04-18 | Full matrix execution coverage lag could cause premature conclusions | High | Block final interpretation until 30/30 runs complete and aggregate tables/figures refreshed | Copilot | Closed (30/30 completed on 2026-04-18)
2026-04-18 | All methods currently show zero success under both reward schemes, so optimal-path objective is not yet met | High | Run targeted failure analysis and controlled retuning (reward scaling, exploration schedule, episode horizon, and observation-ablation checks) before final claim | Copilot | Open
2026-04-19 | Potential-only Reward C (small scale) may be too weak and still preserve stationary local optimum | High | Upgrade to C2 with stronger potential coefficient plus explicit wall-hit and stagnation penalties; verify via 200k break-zero matrix before full-scale runs | Copilot | Mitigating
2026-04-19 | Exploration increase without anti-stagnation penalties can raise return variance but not success rate | Medium | Couple exploration tuning with behavior penalties and monitor failure-mode breakdown (`SPIN_NEAR_START`, `DEEP_WALL_CRASH`) per run | Copilot | Mitigating
2026-04-19 | Over-penalized Reward C2 can collapse return scale without improving success (`~ -101` mean return while success remains 0) | High | Rebalance to C3: keep progress bonus dominant, reduce blanket penalties, and apply penalties only on repeated ineffective actions | Copilot | Open
2026-04-19 | PPO entropy scheduling may break if implemented as callable `ent_coef` in current SB3 version | Medium | Use phase-wise float `ent_coef` updates with explicit phase logging; avoid callable `ent_coef` path | Copilot | Mitigating
2026-04-20 | Pure RL on 16x16 sparse environments leads to catastrophic forgetting/cold-start | High | Introduced A* Path Shaping (Reward E2) and progressive Curriculum Learning (starting agent radius=2 from target) | Copilot | Closed (Demonstrated 1.0% break-zero)
2026-04-21 | Agent forgets early curriculum radius skills when transitioning to global unconstrained grid | High | Acknowledge fundamental PPO/spatial Generalization Bottlenecks in the final report, instead of hacking further hyper-params | Copilot | Mitigating
2026-04-22 | Observation-enrichment ceiling: A* hint (world + ego) accelerates curriculum (k=1→4, radius 12) but fails zero-shot (0%, dead_loop_rate=1.0) | High | Frame Phase 5/5b in final report as a controlled ablation proving observation structure alone is insufficient; explicitly list architectural alternatives (CNN grid, wider MLP, frame-stack) and extended curricula (≥2M, radius≥16) as future work rather than launching more laptop runs | Copilot | Mitigating (Task 3 paused)
2026-04-25 | Deterministic policy collapse after deep curriculum: PPO+F2+CNN reaches level 8 (radius 24) but deterministic final eval still 0.0% with wall/dead-loop rates at 1.0 | High | Treat deterministic and non-deterministic eval as paired diagnostics, keep `--eval-deterministic` explicit in logs, and prioritize lock-state/action-entropy analysis before further reward reshaping | Copilot | Open
2026-04-25 | Evaluation-setting drift (deterministic vs non-deterministic, eval-only vs train+eval, n-eval-episodes mismatch) may cause misleading cross-run comparisons | Medium | Standardize run-log fields (`eval_only`, `eval_deterministic`, `n_eval_episodes`, `resume_from`) and require like-for-like filtering when aggregating conclusions | Copilot | Mitigating
2026-04-26 | Environment drift across reward/observation/topology iterations may make old Task3 runs unfair as final comparisons | High | Freeze Reward G as the final contract and treat older A/B/C/E/F/F2 results as design-history/failure-analysis only | Copilot | Mitigating
2026-04-26 | World-frame observation with ego-frame actions can keep deterministic policies stuck even when A* path information is present | High | Convert 4x7x7 radar to ego-centric coordinates so the top of the matrix is always the robot's forward direction | Copilot | Mitigating
2026-04-26 | Task2 integration may be overstated if entity radar encodes species IDs instead of cluster IDs | Medium | Channel 1 now stores Task2 cluster id + 1 for entity cells, with empty cells kept at 0 | Copilot | Mitigating
2026-04-26 | Task2 cluster lookup can degenerate if raw Task3 species prototypes are compared to standardized Task2 cluster centers | High | Apply the same median-impute + continuous StandardScaler convention before species-to-cluster lookup; retain raw prototypes only for bribe-cost sizing | Copilot | Closed
2026-04-30 | Recurrent PPO on Apple MPS is unstable because the backend hits an LSTM backward assertion | High | Force Stage 7 recurrent runs to CPU and keep the non-recurrent paths on the normal `mps -> cpu` device policy | Copilot | Mitigating
2026-04-30 | The final Task3 comparison can become invalid if DQN is compared before it finishes the same Reward H curriculum used by PPO v5 | High | Freeze the Stage 7 DQN config and environment to match PPO v5, and defer the three-way comparison table until the DQN baseline fully converges | Copilot | Open
2026-04-30 | LSTM improves success but also raises dead-loop and wall-collision rates, which may tempt over-claiming if only success rate is reported | Medium | Keep dead-loop and wall-collision case rates beside success/return in every Stage 7 summary, and inspect failure-mode tables before making any dominant-method claim | Copilot | Mitigating
