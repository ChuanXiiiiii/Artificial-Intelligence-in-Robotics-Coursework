# Progress Tracker

## Overall status
Task | Status | Owner | Last update | Next milestone
---- | ------ | ----- | ----------- | --------------
Task1 Supervised | In progress | Copilot | 2026-04-11 | Keep documentation and artifact index aligned
Task2 Unsupervised | In progress | Copilot | 2026-04-14 | Integrate main vs bribe-ablation findings into final report sections (Implementation/Design/Results/Evaluation)
Task3 Reinforcement | In progress | Copilot | 2026-04-30 | Finish Stage 7 DQN baseline and publish the controlled v4 PPO vs v5 PPO+LSTM vs DQN comparison

## Weekly updates
Date | Task | What was done | Key result | Blocker | Next step
---- | ---- | ------------- | ---------- | ------- | ---------
2026-03-21 | Task1 | Implemented split, HOG+SVM, ResNet18, unified metrics, confusion matrix plots, error analysis, and report sync pipeline | Seed42 test: HOG+SVM Acc=0.8539 Macro-F1=0.6498; ResNet18 Acc=0.8349 Macro-F1=0.6716 | Group-based split causes severe class subgroup imbalance (wingedrat val=1) | Add improved split strategy and run seeds 123/2026 for stability
2026-03-21 | Task1 | Locked runtime to Python 3.11 (.venv311), installed Task1 dependencies, and removed static type warnings in src scripts | Workspace interpreter now uses .venv311 and end-to-end pipeline remains runnable | Split stability risk persists for long-tail subgroup distribution | Prototype alternative split policy with minimum per-class val/test support
2026-04-10 | Task1 | Implemented robust bucket-based split constraints, class-weighting control in ResNet18, and completed seeds 42/123/2026 runs with aggregation | Multi-seed summary: HOG+SVM Acc=0.9652±0.0093 Macro-F1=0.9662±0.0077; ResNet18 Acc=0.9961±0.0046 Macro-F1=0.9958±0.0052 | Minor SVM convergence warning remains on some C values | Keep best artifacts and hand off report-ready figures/tables
2026-04-11 | Task1 | Tuned Method A solver settings (max_iter and dual) and ran controlled before/after 3-seed comparison with warning counts | HOG warning summary unchanged overall (2 -> 2), but mean Acc/F1 improved (Acc 0.9641 -> 0.9652, Macro-F1 0.9643 -> 0.9663) | Seed-dependent warning persistence still exists (seed42/2026 after tuning) | Optionally test larger max_iter (50000) and alternative C grid for full warning elimination
2026-04-11 | Task1 | Finalized interpretation of Method A convergence warning using before/after tables and froze current tuning scope | ConvergenceWarning does not change Task1 final comparison conclusion under seeds 42/123/2026 | Warning not fully removed in every seed, but performance remains stable | No 50000-iteration expansion; keep docs and report assets synchronized
2026-04-14 | Task2 | Implemented full unsupervised pipeline (shared preprocessing, K-Means, GMM, dual-view compare, PCA/UMAP, multi-seed aggregation) and ran seeds 42/123/2026 | Multi-seed summary stable; at fixed k=5 K-Means slightly outperforms GMM on Silhouette/DBI/CHI, method-best selects k=6 for both methods | Need to explain why k=6 can be valid when task has 5 species and keep fairness narrative explicit | Add report-ready explanation in Task2 README and keep dual-view tables in final report
2026-04-14 | Task2 | Updated management logs (decisions/risks) and enforced preprocessing-once in multi-seed script | Risk controls now aligned with implementation: fairness dual-view, six reproducibility fields, metric-first interpretation over UMAP visuals | Bribe feature impact on conclusions still not isolated | Run dedicated bribe ablation in separate output root and compare against main experiment
2026-04-14 | Task2 | Completed dedicated with-bribe ablation in isolated output root (`outputs_bribe`) across seeds 42/123/2026 and aggregated results | With-bribe metrics are consistently higher (e.g., fixed-k silhouette 0.5578 -> 0.6142 for K-Means), indicating shortcut-like separability inflation while method ranking remains similar | Need to ensure report does not over-claim gains from leakage-prone feature | Keep no-bribe as primary conclusion; use with-bribe as sensitivity appendix
2026-04-16 | Task3 | Started implementation: wrapper-based env integration (Task1 entity hints + Task2 sensor/cluster features), Random/DQN/PPO training entry, multi-seed aggregator, sync script, configs, and notebook journal | Implementation contract fixed for robust evaluation: methods={Random,DQN,PPO}, rewards={A,B}, seeds={42,123,2026,9,20}, timesteps=1M per method/seed | Full matrix runtime on laptop may be long; dependency install and first smoke run still pending | Run smoke training for one seed/reward per method, then launch full 5-seed matrix and aggregate mean/std curves
2026-04-18 | Task3 | Completed engineering chain hardening: MPS runtime checks, long-run decay configs, thermal-safe batched queue (2-3 parallel + 30s cooldown), strict trajectory export, artifact validation, and robust logger handling | Smoke validation passed for random/dqn/ppo (seed42, rewardA); strict export behaves as designed (fails when model missing, passes after artifact backfill) | Full matrix (30 runs) still not executed end-to-end due runtime budget | Start official full matrix command: `bash src/run_task3_multi_seed.sh 1000000 2 30 true`
2026-04-18 | Task3 | Added required per-run outputs and reproducibility surfaces (metrics, curve, progress, loss, best_model, run_log, reflection cases, CI aggregate, best-trajectory summary) | Current artifact completeness for completed runs is 100% (all required files present for available runs) | Experimental coverage remains partial (3/30 combinations complete) | Finish remaining 27 combinations and refresh multi-seed report tables/figures
2026-04-18 | Task3 | Completed official full matrix execution (`1M steps x 30 runs`) with strict export and report sync pipeline | 30/30 runs passed artifact checks; trajectory export succeeded for all method/reward pairs; synced figures/tables to report folders | Aggregate success remains 0.0 for all methods under both reward schemes in current setup | Run failure-mode diagnosis and targeted retuning (reward shaping, horizon, exploration, and observation simplification)
2026-04-19 | Task3 | Completed failure-mode behavioral audit from six exported best trajectories (A/B x random/dqn/ppo) and replay-based coordinate classification | Dominant failures are SPIN_NEAR_START and wall-heavy stagnation; LOOP_TWO_CELL is not dominant in this version | Agent still exploits low-risk local behavior and does not reach target | Add stronger anti-stagnation shaping and explicit wall penalty in next reward iteration
2026-04-19 | Task3 | Added Reward C (potential-based shaping), increased DQN exploration (fraction/final eps), and raised PPO entropy coefficient, then ran break-zero quick matrix (200k, seed42, A/B/C x DQN/PPO) | Break-zero failed in all 6 groups (`success_rate=0.0`); Reward C with scale 0.05 is too weak to escape local optima | Current shaping still allows stationary/near-start policy to survive training | Execute C2 iteration: stronger potential scale + wall/stagnation penalties + PPO entropy schedule and revalidate quickly
2026-04-19 | Task3 | Implemented C2 reward iteration (stronger potential + wall/dead-loop penalties), fixed PPO entropy scheduling bug, and reran 200k quick matrix for seed42 | PPO runs are stable again after replacing callable `ent_coef` with phase-wise float updates; break-zero still 0/6; Reward C return collapsed (DQN -100.9, PPO -101.581) | Penalty scale in C2 is too aggressive and overwhelms navigation signal | Move to C3: rebalance reward magnitudes, penalize only repeated ineffective forward collisions, and keep positive progress term dominant
2026-04-20 | Task3 | Introduced Reward E2 (A* Shortest Path Shaping) with heavy penalties | Agent learned collision avoidance but completely failed to explore globally (0% success) | Pure RL on 16x16 sparse map suffers from extreme cold-start problem | Introduce Curriculum Learning wrapper
2026-04-20 | Task3 | Implemented Curriculum Learning (gradually increasing spawn radius) | Fast curriculum (300k steps, radius-step 5) successfully broke zero! (1.0% success rate on test map) | Agent undertrained on intermediate radii | Scale up to 2 Million step Master Curriculum
2026-04-21 | Task3 | Ran 2,000,000 step Master Curriculum run (radius-step 2, threshold 80%) | Agent perfectly mastered local navigation (holding 80%+ success on radius<=8) but generalized poorly (0.0% final zero-shot success) | Demonstrates Generalization Bottleneck and Catastrophic Forgetting in PPO for long-horizon spatial tasks | Freeze Task 3 experiments; document the Curriculum and A* limitations as the final deep-RL conclusion
2026-04-21 | Task3 | Diagnosed Phase-4 generalisation failure as information-structural: 24-d MLP observation has no wall-topology channel, forcing policy to memorise walls through hidden state | Designed Reward F = E2 + 2-d A* next-step direction `(dx, dy)` injected into observation (obs dim 24→26), analogous to HER sub-goal conditioning | Observation-space changes invalidate old checkpoints; need fresh run | Implement `include_astar_hint` flag in `hero_task3_env.py`, wire through `build_env_kwargs`, add `configs/env_stage4_rewardF.json`
2026-04-21 | Task3 | Implemented Reward F end-to-end and ran 5k-step smoke test under `--use-curriculum false` | Pipeline works with 26-dim obs; PPO trains without errors; return -216 at 5k as expected under dense penalties and untrained policy | No success signal yet (too short) | Launch 500k-step curriculum run with radius-step 3 / window 50 / threshold 0.75 for quick validation
2026-04-21 | Task3 | Completed Reward F 500k curriculum run (world-frame A* hint only) | Only 1 curriculum level-up (k=1→2, plateau at radius 8, 46–60% in-stage success); zero-shot 1.0% | World-frame (dx,dy) still forces MLP to learn the ego-rotation mapping internally | Add an additional 3-d ego-frame one-hot `[is_forward, is_left, is_right]` (Reward F2) to bypass the world/ego coordinate gap
2026-04-22 | Task3 | Implemented Reward F2 (world + ego A* hint, obs dim 29) and launched 1M-step curriculum (radius-step 3, window 50, threshold 0.60) | **3 curriculum level-ups (k=1→2→3→4 reaching radius 12)** with 38–42% in-stage success — ego hint clearly accelerates progression vs Reward F (1 level-up). Zero-shot evaluation still 0.0% (dead_loop_rate=1.0) | Observation enrichment solves the *what-to-learn* signal but the gap between curriculum max radius (12) and full-map evaluation radius blocks generalisation; horizon length and accumulated planning error dominate at test time | Stop investment in Task 3 for now; document Phase-5 findings in final report; if revisited, try longer curriculum (≥2M, radius≥16), wider MLP, CNN on 2-D sensor grid, or frame-stacking
2026-04-25 | Task3 | Completed topology/observation/policy refactor: braid-maze opening in reset, 4-channel local radar observation `(4,7,7)`, and unified PPO/DQN `CnnPolicy + TinyCNN` for fair visual comparison | RL stack now supports tiny-grid CNN training without NatureCNN shape issues; DQN and PPO share the same observation/extractor family for fairness | Observation/policy migration invalidates older MLP checkpoints and requires fresh runs for comparability | Run full F2 curriculum with new visual pipeline and capture deterministic vs non-deterministic evaluation deltas
2026-04-25 | Task3 | Ran PPO+F2 curriculum for 2,000,000 steps under new CNN pipeline (`f2_cnn_curriculum_2m`) | Curriculum progressed to **level=8/9, effective_radius=24** (11 level-up/bridge events), showing much deeper training coverage than prior radius-12 ceiling | Final deterministic evaluation remains collapsed: `success_rate=0.0`, `dead_loop_rate=1.0`, `wall_collision_case_rate=1.0` | Add eval-only resume diagnostics to separate learning-quality issues from deterministic action-selection collapse
2026-04-25 | Task3 | Added checkpoint evaluation controls (`--eval-only`, `--resume-from`, `--eval-deterministic`) and ran non-deterministic eval-only smoke on 2M checkpoint | Same checkpoint reaches `success_rate=0.05` on 20 episodes with `--eval-deterministic false`, while deterministic remains 0.0, supporting deterministic-collapse hypothesis | Sample size is still small (20-episode smoke), so confidence interval is wide | Execute larger non-deterministic A/B (e.g., 100-500 eps) and compare action-entropy/lock-state statistics before any new reward redesign
2026-04-26 | Task3 | Froze final environment contract as Reward G: ego-centric 4x7x7 radar, Task2 cluster entity channel, dynamic bribe costs, stronger wall/stagnation/dead-loop penalties, final curriculum launcher, and Task2-standardized species-to-cluster mapping | Fixes the largest discovered mismatches: previous CNN radar was world-frame while actions are ego-frame, Channel 1 encoded species IDs instead of Task2 clusters, and raw prototypes were being compared against standardized Task2 centers | No new long run has been launched yet; success still must be validated by fresh training from scratch | Run `bash src/run_task3_final_curriculum.sh 42 2000000 100 finalG_curriculum`, then evaluate deterministic and non-deterministic success separately
2026-04-27 | Task3 | Implemented Reward H stealth extension on top of the frozen CNN pipeline and completed the 6M PPO Stage 6 run | The first strong post-freeze Task3 policy appeared: seed42 PPO reached `success_rate=0.32` with low dead-loop (`0.04`) and wall-collision (`0.02`) rates | Reward H is still partially observable, so the agent remains memory-limited under long-horizon starts | Upgrade PPO to a recurrent policy while keeping Reward H physics fixed
2026-04-30 | Task3 | Added RecurrentPPO support (`--use-recurrent`, `--lstm-hidden-size`, `--n-lstm-layers`), refactored evaluation/GIF export for hidden-state inference, and completed the 6M Stage 7 PPO+LSTM run | Seed42 v5 improved to `success_rate=0.41` and `episode_return_mean=-44.34`; layered eval reached `k1=0.91`, `k2=0.81`, `k3=0.73`, `standard_start=0.33` | Recurrent PPO on MPS is unstable because of an LSTM backward bug, so the recurrent path must use CPU | Keep DQN on the exact same Reward H curriculum to complete the fair algorithm comparison
2026-04-30 | Task3 | Created the Stage 7 DQN fairness-baseline config/launcher, enabled TensorBoard logging for DQN, and launched the 6M baseline run; also exported paired marker/sprite GIFs for the v5 LSTM checkpoint | The comparison pipeline is now structurally complete and the report has paired visuals from the same successful rollout (`stage7_lstm_standard_markers.gif`, `stage7_lstm_standard_sprites.gif`) | DQN has not converged yet, so the final three-way table cannot be frozen | Wait for DQN convergence, then summarize v4 PPO vs v5 PPO+LSTM vs DQN in the report assets
