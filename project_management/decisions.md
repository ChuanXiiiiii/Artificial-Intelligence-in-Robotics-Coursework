# Decisions Log

Date | Scope | Decision | Why | Impact
---- | ----- | -------- | --- | ------
YYYY-MM-DD | Task1 | Use HOG+SVM as Method1 | Strong baseline on small images | Fast baseline and interpretable errors
2026-04-11 | Task1 | Stop further max_iter expansion for Method A (no 50000 run) and accept convergence-warning interpretation for current scope | Before/after 3-seed comparison shows stable or slightly improved metrics without conclusion reversal | Keeps schedule focused while preserving evidence-backed conclusion quality
2026-04-14 | Task2 | Use dual-view fairness protocol (method-best + fixed-same-k) for K-Means vs GMM | Prevent unfairness criticism from only per-method optimum | Improves validity and auditability of comparison
2026-04-14 | Task2 | Lock three-seed protocol (42,123,2026) and unified preprocessing for both methods | Ensure reproducibility and stability assessment | Supports robust cross-seed conclusions
2026-04-14 | Task2 | Explicitly report why method-best may choose k=6 while task semantics reference 5 species | Unsupervised internal structure and labeled species count are related but not identical objectives | Preserves scientific validity while keeping assignment-aligned fixed-k view
2026-04-14 | Task2 | Keep `bribe` excluded in main experiment and evaluate it only in separate ablation outputs | `bribe` may inject shortcut-like separability and inflate clustering quality | Reduces leakage risk in main conclusion and makes sensitivity analysis auditable
2026-04-14 | Task2 | After ablation, keep no-`bribe` pipeline as primary result and with-`bribe` as sensitivity-only appendix | With-`bribe` scores improve markedly but may overstate practical separability | Prevents leakage-driven over-claims while preserving transparent robustness analysis
2026-04-16 | Task3 | Use wrapper-only extension strategy on top of DungeonMazeWorld baseline | Requirement asks baseline consistency and auditability; wrapper keeps core env untouched | Lowers integration risk and keeps environment diffs minimal
2026-04-16 | Task3 | Use Random baseline + DQN + PPO (SB3) as mandatory comparison set | Covers baseline, value-based, and policy-based paradigms in one unified pipeline | Supports theory-driven comparison with reproducible engineering effort
2026-04-16 | Task3 | Lock five-seed protocol to 42/123/2026/9/20 and aggregate with mean/std | User requested robust evaluation with five seeds and variance-aware reporting | Ensures final figures/tables directly satisfy robustness section
2026-04-16 | Task3 | Adopt two reward schemes (A sparse, B dense heuristic with hostile penalty) | Required by assignment brief and needed for convergence behavior comparison | Enables reward-engineering ablation with shared algorithm settings
2026-04-16 | Task3 | Use hostile=orc/lizard/wingedrat and bribable=human/halfling semantic mapping | Task1 label space lacks explicit bribable class; mapping needed for actionable policy | Makes entity-conditional strategy computable without changing Task1 taxonomy
2026-04-16 | Task3 | Prefer torch `mps` device with automatic CPU fallback | Target hardware is MacBook Air M4 and user requested MPS acceleration | Improves training speed while preserving compatibility if MPS unavailable
2026-04-18 | Task3 | Enable strict trajectory-export mode in official full-matrix runner by default (`STRICT_EXPORT=true`) | Report-grade runs must fail fast on missing metrics/model artifacts instead of silently skipping | Increases auditability and prevents incomplete visual evidence from entering final report
2026-04-18 | Task3 | Use thermal-safe batched queue execution (max parallel in [1,3], default 2, cooldown 30s) | MacBook Air is fanless; uncontrolled parallelism risks throttling and unstable runtime characteristics | Better hardware stability and more consistent timing across batches
2026-04-18 | Task3 | Apply long-run decay strategy for 1M-step training (DQN phased LR+buffer decay, PPO LR decay schedule) | Late-stage convergence quality may degrade with fixed aggressive settings | Improves long-horizon optimization stability while retaining reproducible config surface
2026-04-18 | Task3 | Make logging resilient to empty SB3 progress CSV files and preserve per-run loss/progress artifacts | DQN phased logger can emit empty files in short phases, causing brittle post-processing | Prevents avoidable run crashes and guarantees required outputs (`loss_*.csv`, `progress_*.csv`)
2026-04-18 | Task3 | Treat the completed 30-run matrix as a baseline diagnostic, not final optimal-path evidence | Full matrix completed with strict checks, but all method/reward groups show `success_rate_mean=0.0` | Next phase is targeted failure analysis and retuning before claiming navigation optimality
2026-04-19 | Task3 | Introduce Reward C as potential-based shaping (`+0.05 * (d_prev - d_curr)`) and expand quick matrix to A/B/C for seed42 at 200k steps | Need a low-cost test for whether distance-aligned shaping can break zero-success regime | Added reproducible break-zero baseline for reward-engineering iteration
2026-04-19 | Task3 | Keep DQN in high-exploration mode (higher `exploration_fraction` and slower epsilon decay) and raise PPO entropy regularization for early-stage exploration | Behavioral audit indicates under-exploration and premature local policy lock-in near start/boundary | Increases exploration pressure without changing algorithm family
2026-04-19 | Task3 | Move to C2 iteration after Reward C quick matrix failed (0/6 break-zero): add explicit wall/stagnation penalties and scheduled PPO entropy decay | Potential shaping alone did not alter success rate and local minima persisted | Establishes next controlled tuning axis before scaling back to multi-seed 1M runs
2026-04-19 | Task3 | Implement PPO entropy scheduling via phase-wise `model.ent_coef` float updates (not callable `ent_coef`) | SB3 PPO training failed with `TypeError` when `ent_coef` was set to a function | Restored PPO training stability for A/B/C quick runs
2026-04-19 | Task3 | Reject current C2 coefficient set as default training configuration | C2 quick matrix still 0/6 break-zero and Reward C returns degraded sharply (`dqn=-100.9`, `ppo=-101.581`) | Next iteration must rebalance penalty/bonus scales before any multi-seed expansion
2026-04-20 | Task3 | Introduce Reward E2 (A* Path Shaping) | Replacing Manhattan distance with true A* shortest-path distance + strict penalties (-0.3 wall, -0.1 dead loop, -0.05 stagnation) | Provides physically accurate path guidance.
2026-04-20 | Task3 | Implement Curriculum Learning Wrapper | Agent heavily struggled with the cold-start exploration problem under severe penalties | Agent is initialized near the goal, progressively moving further away only when success rate > 80%
2026-04-20 | Task3 | Fast Curriculum Run (300k steps, radius-step 5) | Test if the curriculum wrapper can break zero | Successfully broke zero! Reached 1.0% success rate on the test map.
2026-04-21 | Task3 | Ultra-smooth Master Curriculum Run (2M steps, radius step 2, threshold 80%, window 100) | Attempt to push the success rate above 80% on the final map | Agent learned local radii brilliantly but failed zero-shot global evaluation (0.0%), proving Catastrophic Forgetting and Generalization Bottlenecks in pure spatial PPO.
2026-04-21 | Task3 | Introduce Reward F = E2 + A* next-step direction injected into observation (obs dim 24→26) | Phase-4 failure is diagnosed as an observability problem, not an optimisation problem: MLP has no wall channel and cannot plan globally. Injecting A* next-step direction reduces the task from global path planning to local action matching, analogous to HER sub-goal conditioning and AlphaStar scripted macros | Requires adding `include_astar_hint` flag to env, bumping obs dim, and invalidating prior E/E2 checkpoints. No extra reward hacks - changes confined to observation structure so any uplift is cleanly attributable.
2026-04-21 | Task3 | Extend to Reward F2 = F + 3-d ego-frame one-hot `[is_forward, is_left, is_right]` (obs dim 26→29) | Reward F 500k reached only k=2 (radius 8); diagnosis: world-frame `(dx,dy)` still requires MLP to learn the ego-rotation mapping from current heading `agent_dir` to the three ego actions. Supplying the ego-projected hint directly removes this learning burden | Obs dim increases again, checkpoints non-transferable; but expected to accelerate curriculum progression without changing reward dynamics
2026-04-22 | Task3 | Pause Task 3 experimental iteration after Phase 5b | Reward F2 1M-step curriculum reached k=4 (radius 12) with 3 level-ups — clear evidence that ego-frame hint helps — yet zero-shot evaluation is still 0.0% with `dead_loop_rate=1.0`. Observation enrichment addresses the *policy-learning* bottleneck but not the *horizon-scale* generalisation bottleneck | Final report will frame Phase 5/5b as a controlled ablation: observation structure alone is insufficient; further gains would require architectural changes (CNN over 2-D sensor grid, wider MLP, or frame-stacking) or substantially longer curricula (≥2M steps, radius ≥16). No additional compute committed now.
2026-04-25 | Task3 | Replace MLP observation/policy path with a unified visual stack: braid-maze topology + 4-channel `(4,7,7)` observation + `CnnPolicy` with custom `TinyCNN` for both DQN/PPO | Prior 1-D vector + MLP path could not preserve local wall topology and made DQN/PPO comparisons architecturally asymmetric after PPO-side changes | Establishes a fair, shared visual pipeline and removes NatureCNN shape incompatibility for tiny inputs
2026-04-25 | Task3 | Standardize active reward scheme to F2 in batch/config defaults | Current diagnosis loop is centered on F2 (world + ego A* hints), and mixed defaults increase accidental config drift | `configs/experiment.json` and `src/run_task3_multi_seed.sh` now default to F2, improving run reproducibility and command consistency
2026-04-25 | Task3 | Add `--eval-only`, `--resume-from`, and `--eval-deterministic` to training entry and treat deterministic vs non-deterministic evaluation as a required diagnostic pair | 2M CNN+F2 run reaches deep curriculum levels but deterministic final success remains zero; we need a direct test to isolate policy-collapse effects | Non-deterministic eval-only already shows non-zero success (`0.05` on 20 episodes), so future conclusions must report both settings before claiming full failure
2026-04-26 | Task3 | Reset the final main training line to a frozen Reward G environment instead of continuing to mutate F2 | F/F2/CNN changes altered the MDP over time, making older checkpoints and comparisons unsuitable as final evidence. A clean contract is needed before retraining | Older Task3 runs are retained as design-history and failure-diagnosis evidence; final claims should use Reward G runs only
2026-04-26 | Task3 | Make the 4x7x7 CNN observation ego-centric and encode Task2 cluster IDs in Channel 1 | The previous local radar was world-frame while the action space is turn-left/turn-right/forward, and Channel 1 still used species IDs rather than clusters | Reduces world-to-ego translation burden and aligns implementation with the stated Task2 integration rule
2026-04-26 | Task3 | Standardize Task3 species prototypes before nearest-cluster lookup | Task2 K-Means centers are in standardized feature space; comparing raw Task3 prototypes to those centers collapsed all species into one cluster | Restores meaningful cluster IDs (`halfling=0`, `human=5`, `lizard=1`, `orc=3`, `wingedrat=2` under seed42/k6)
2026-04-26 | Task3 | Use dynamic low bribe costs and strong hostile/wall/loop penalties in Reward G | The tactical goal is that short detours beat hostile contact, while crossing bribable/neutral entities can beat long detours | Reward scores are now explicit and auditable in `configs/env_final_rewardG.json` and `TASK3_FINAL_RULES.md`
2026-04-27 | Task3 | Extend Reward G into Reward H stealth mode with dynamic patrol enemies, kill-zone terminal penalty, weapon-state channel, and frame stacking | Reward G fixed the CNN contract, but it still treated entities as mostly static local costs; stealth needed temporal cues and harder entity avoidance | Creates the first post-freeze environment extension and moves the effective network input to `15x7x7` (`5x7x7` stacked across 3 frames)
2026-04-30 | Task3 | Hold Reward H physics fixed and introduce Stage 7 recurrent PPO (`RecurrentPPO + CnnLstmPolicy + TinyCNN`) as the primary memory upgrade | Reward H remains partially observable under a 7x7 local view; adding recurrent state is the cleanest test of the object-permanence / memory hypothesis | Produced the current best seed42 result (`success_rate=0.41`) and establishes a defensible architectural comparison against non-recurrent baselines
2026-04-30 | Task3 | Force recurrent PPO to CPU on Apple Silicon instead of using MPS | The MPS backend hits a known LSTM backward assertion during recurrent PPO training, so leaving device selection automatic would make Stage 7 unstable | Keeps recurrent runs reliable at the cost of slower throughput; non-recurrent PPO/DQN still use the normal `mps -> cpu` fallback
2026-04-30 | Task3 | Launch a Stage 7 DQN fairness baseline under the exact same Reward H environment and curriculum as PPO v5 | The final comparison must isolate algorithm family, not benefit from environment drift or different curriculum schedules | Final three-way comparison is now well-posed, but it remains pending until the DQN run converges
